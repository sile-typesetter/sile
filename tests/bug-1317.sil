\begin[direction=RTL,papersize=a7]{document}
\font[family=Amiri,size=30pt]
\nofolios
\neverindent
\begin{script}

SILE.documentState.documentClass:declareFrame("other", {
    left = "left(content) + 20%pw",
    right = "right(content) - 20%pw",
    top = "top(content) + 20%ph",
    bottom = "bottom(content) - 20%ph",
    direction = "RTL"
  })

SILE.typesetNaturally(SILE.getFrame("other"), function()
  SILE.typesetter:typeset("عَرَبي")
end)

SILE.typesetter:typeset("عَرَبي")

\end{script}
\end{document}
