-- Set of imported modules.

return {
  -- true => module symbols injected into equivalent core namespace
  --         with `require 'std'`:
  debug      = true,
  debug_init = false,
  functional = false,
  io         = true,
  list       = false,
  math       = true,
  optparse   = false,
  package    = true,
  set        = false,
  strbuf     = false,
  string     = true,
  table      = true,
  tree       = false,
}
